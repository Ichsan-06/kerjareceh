version: '3.8'

services:
  # 1. Aplikasi PHP (Laravel)
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: recehan-app-image
    container_name: recehan-app
    restart: unless-stopped
    working_dir: /var/www
    volumes:
      - ./:/var/www
    networks:
      - recehan

  # 2. Web Server (Nginx)
  webserver:
    image: nginx:alpine
    container_name: recehan-nginx
    restart: unless-stopped
    ports:
      - "8083:80"
    volumes:
      - ./:/var/www
      - ./docker/nginx/conf.d/:/etc/nginx/conf.d/
    networks:
      - recehan

  # 3. Database (MySQL)
  db:
    image: mysql:8.0
    container_name: recehan-db
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: ${DB_DATABASE}
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
      # MYSQL_PASSWORD: bukudigital
      # MYSQL_USER: ${DB_USERNAME}
      SERVICE_TAGS: dev
      SERVICE_NAME: mysql
    volumes:
      - dbdata:/var/lib/mysql
    networks:
      - recehan

  # 4. PHPMyAdmin
  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    container_name: recehan-pma
    links:
      - db
    environment:
      PMA_HOST: db
      PMA_PORT: 3306
      PMA_ARBITRARY: 1
    restart: always
    ports:
      - 8084:80
    networks:
      - recehan

networks:
  recehan:
    driver: bridge

volumes:
  dbdata:
    driver: local
